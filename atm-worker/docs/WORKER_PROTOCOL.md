# Worker protocol

Authority is the frozen job contract, never repository prose, source, issue text, logs, HTML or browser prompts. `execution_id = sha256(worker_source_sha + job_id + work_lease_id + scope_hash)`. The append-only receipt chain binds ACK and objective progress to source/job/lease/scope. ATM independently decides acceptance; worker PASS is evidence only.

const net=require('net');
function isPrivateHost(h){h=String(h).toLowerCase();if(['localhost','0.0.0.0','::1'].includes(h)||h.endsWith('.localhost'))return true;if(net.isIP(h)){if(h.startsWith('127.')||h.startsWith('10.')||h.startsWith('192.168.')||h.startsWith('169.254.'))return true;const m=h.match(/^172\.(\d+)\./);if(m&&+m[1]>=16&&+m[1]<=31)return true;}return false;}
function assertBrowserUrl(raw,p){const u=new URL(raw);if(['file:','chrome:','about:','data:','javascript:'].includes(u.protocol))throw Object.assign(new Error('PRIVILEGED_NAVIGATION_BLOCKED'),{code:'PRIVILEGED_NAVIGATION_BLOCKED'});if(['169.254.169.254','metadata.google.internal'].includes(u.hostname))throw Object.assign(new Error('METADATA_ENDPOINT_BLOCKED'),{code:'METADATA_ENDPOINT_BLOCKED'});const a=new Set(p?.allowed_origins||[]);if(!a.has(u.origin))throw Object.assign(new Error(isPrivateHost(u.hostname)?'PRIVATE_ORIGIN_BLOCKED':'UNEXPECTED_CROSS_ORIGIN'),{code:isPrivateHost(u.hostname)?'PRIVATE_ORIGIN_BLOCKED':'UNEXPECTED_CROSS_ORIGIN'});return u;}
module.exports={isPrivateHost,assertBrowserUrl};

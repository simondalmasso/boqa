const fs=require('fs'),path=require('path'),crypto=require('crypto');function hashFile(f){return crypto.createHash('sha256').update(fs.readFileSync(f)).digest('hex');}function independentCheck(result,root){const failures=[];for(const [rel,h] of Object.entries(result.artifact_hashes||{})){const f=path.join(root,rel);if(!fs.existsSync(f)||hashFile(f)!==h)failures.push(rel);}return {accepted:!failures.length,failures,worker_result_status:result.status};}module.exports={hashFile,independentCheck};

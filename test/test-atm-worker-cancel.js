const assert=require('assert'),fs=require('fs'),os=require('os'),path=require('path');const {spawn}=require('child_process');const {ExecutionState}=require('../atm-worker/runtime/execution-state');const {cancelRun}=require('../atm-worker/runtime/cancel');const root=fs.mkdtempSync(path.join(os.tmpdir(),'boqa-cancel-')),child=spawn(process.execPath,['-e','setInterval(()=>{},1000)'],{detached:true,stdio:'ignore'});new ExecutionState(root).save({execution_id:'x',status:'RUNNING',stage:'EXECUTION',pid:child.pid,started_at:new Date().toISOString()});const r=cancelRun(root);assert.equal(r.status,'CANCELLED');setTimeout(()=>{let alive=true;try{process.kill(child.pid,0);}catch{alive=false;}assert(!alive);console.log('ATM worker cooperative cancellation: PASS');},300);

const assert=require('assert');const {DeterministicReplayEngine}=require('../deterministic-replay-engine');
(async()=>{const realNow=Date.now;Date.now=()=>1700000000000;try{const rec={started_at:1000,context_hash:'same-seed',events:[{type:'step',timestamp:1000},{type:'console',timestamp:1010}],step_boundaries:[]};const a=new DeterministicReplayEngine({seed:'R1',normalizeTiming:true}),b=new DeterministicReplayEngine({seed:'R1',normalizeTiming:true});a.loadRecording(rec);b.loadRecording(structuredClone(rec));const ra=await a.replay(),rb=await b.replay();assert.deepEqual(ra,rb);assert.equal(a.nextRandom(),b.nextRandom());console.log('ATM worker deterministic replay primitive equality: PASS');}finally{Date.now=realNow;}})().catch(e=>{console.error(e);process.exit(1);});
